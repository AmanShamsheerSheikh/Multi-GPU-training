{
  "training_type": "ddp",
  "max_steps": 400,
  "warmup_steps": 20,
  "save_every_n_steps": 200,
  "upload_every_n_steps": 200,
  "job_id": "benchmark-ddp-gpt2-xl",
  "model_name": "openai-community/gpt2-xl",
  "job_type": "train",
  "dataset_config": {
    "dataset_name": "roneneldan/TinyStories",
    "task_type": "casual_lm",
    "columns": ["text"],
    "max_length": 512
  },
  "epochs": 1,
  "gpu_count": 1,
  "batch_size": 16,
  "accumulation_steps": 4,
  "hf_repo_id": "",
  "dataloader_workers": 4
}